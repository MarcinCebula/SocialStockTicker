describe 'SocialStock', ->


#	beforeEach ->
#		MyApp.student.set 'firstName', 'Franek'
#		MyApp.student.set 'lastName', 'Kapusta'
#		MyApp.book.set 'title', 'Muminki'
#	describe 'student', ->
#		Ember.run ->
#			it 'should have attribute "firstName" with name "Franek"', ->
#				expect(MyApp.student.get('firstName')).toEqual('Franek')
#			it 'should have attribute "lastName" with name "Kapusta"', ->
#				expect(MyApp.student.get('lastName')).toEqual('Kapusta')
#			it 'should not have attribute "firstName" with name "Marcin"', ->
#				expect(MyApp.student.get('firstName')).not.toEqual('Marcin')
#			it 'should return "Franek Kapusta for fullName()"', ->
#				expect(MyApp.student.get('fullName')).toEqual('Franek Kapusta')
#		describe 'book', ->
#			Ember.run ->
#				it 'should have attribute "title" equal "Muminki"', ->
#					expect(MyApp.book.get('title')).toEqual('Muminki')
